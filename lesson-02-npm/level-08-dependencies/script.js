import chalk from "chalk";

console.log(
  chalk.cyan(
    "Dependencies in package.json are packages a project is need to run; the projects imports dependencies and uses them by setting type to module",
  ),
);
