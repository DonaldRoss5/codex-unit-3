const definedMessage =
  "T o use export default, Define a value. Save it in a variable. Use export default and the variable name to mkae it accessible to other files";

export default definedMessage;


// 1. create variable = "string";

// 2. export default definedMessage (makes it accessible)