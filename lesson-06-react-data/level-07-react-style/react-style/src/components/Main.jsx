import { Slides } from "./Slides";

function Main() {
  return (
    <main>
      <p>
        To render tw elements components: install packages, initialize tailwind,
        edit config file, add style. css to connect tailwind and tw eleements,
        make component to return tw elements code, fix errors differences btw js
        and html, use js code in mount phasev{" "}
      </p>

      <Slides />
    </main>
  );
}
export default Main;
