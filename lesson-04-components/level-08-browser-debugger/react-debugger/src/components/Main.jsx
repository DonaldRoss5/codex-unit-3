export default function Main() {
  const message =
    "Start vite server, open the Run and Debug panel, select React Debugger from the dropdown and press play";

  return (
    <main>
      <p>{message}</p>
    </main>
  );
}
