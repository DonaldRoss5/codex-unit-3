export default function Main() {
  const message =
    "the debugger must first be manually configured by editing the settings file in JSON format, so its content is just like an object, each property is in quotations. must be manually configured by editing the settings file.The settings file is in JSON format, so its content is just like an object, except that each property is in quotations. Open the Run and Debug panel, select React Debugger from the dropdown and press play";

  return (
    <main>
      <p>{message}</p>
    </main>
  );
}
