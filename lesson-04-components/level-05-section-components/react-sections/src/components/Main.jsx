export function Main() {
  return (
    <main>
      <p>
        To convert sections into React components: For the header section, let
        the Header Function return a the header tag. For the main section, let
        the Main function return the main tag. For the footer section let a
        function return the footer tag.
      </p>
    </main>
  );
}
