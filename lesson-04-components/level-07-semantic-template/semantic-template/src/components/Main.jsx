export default function Main() {
  return (
    <main>
      <p>
        To make semati template create a body tag that has semantic HTML:
        header, main, and footer. A script tag should have the defer attribute
        when in the head tag.
      </p>
    </main>
  );
}
