console.log(
  "To run the JS files in the terminal, complete the task then type node + file.",
);
console.log(
  "use the cd command to navigate to specific folder and and node to run the file ",
);
